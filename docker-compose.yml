# Build all images and run all containers
# `docker-compose -f docker-compose.yml build`

version: '3.7'

services:
  app:
   image: registry.lts.harvard.edu/lts/list_view:2.0.12
   build:
     context: .
     dockerfile: Dockerfile

